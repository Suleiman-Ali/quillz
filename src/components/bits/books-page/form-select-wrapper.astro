---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<select
  {name}
  class="p-3 w-full max-w-36 text-base text-zinc-700 bg-zinc-100 rounded outline-none border-none appearance-none"
  ><slot /></select
>
